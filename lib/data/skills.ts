// Skills data now lives in the database. Use the query layer:
//
//   import { getAllSkills, getSkillTabs } from "@/lib/queries"   (server only)
//
// On the client, skills are hydrated into the Zustand store by <DataProvider>.

export {};
